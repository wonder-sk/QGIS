language: cpp
compiler: gcc
before_install:
  - sudo apt-get update
  - sudo apt-get install -y libgdal1h libgdal-dev libproj-dev libqt4-dev python-qt4 python-qt4-dev pyqt4-dev-tools
script:
  - mkdir build
  - cd build
  - cmake ..
  - make

